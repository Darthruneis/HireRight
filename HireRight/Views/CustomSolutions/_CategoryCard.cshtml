@using System.Text
@model JobAnalysisCategoryViewModel

@{
    var rank = 1;
    var className = new StringBuilder("categoryCard");
    className.Append($" {Model.Importance}");
    var style = rank < 1 ? "display: none;" : "";
    foreach (IndustryDTO industry in Model.AllIndustries)
    {
        if (Model.RelevantIndustries.Contains(industry.Id))
        {
            className.Append($" industry-{industry.Name}");
        }
    }
}
<div class="@className.ToString()" data-categorytitle="@Model.Title" data-visibilityrank="@rank" style="@style">
    <div class="row">
        <div class="col-xs-12">
            <div class="cardDetails">
                <div class="cardHeader">
                    <label>@Model.Title</label><br />
                </div>
                @Model.Description
            </div>
        </div>
        <div class="col-xs-12">
            <div class="row">
                <div class="col-xs-6" style="padding-right: 0;">
                    <div class="pull-left importanceToggleButton lessImportantButton" style="display: @(Model.Importance == CategoryImportance.Irrelevant ? "none" : "")">
                        <i class="glyphicon glyphicon-arrow-left"></i>
                    </div>
                </div>
                <div class="col-xs-6" style="padding-left: 0;">
                    <div class="pull-right importanceToggleButton moreImportantButton" style="display: @(Model.Importance == CategoryImportance.HighImportance ? "none" : "")">
                        <i class="glyphicon glyphicon-arrow-right"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
